
<div class="container">
	<div class="row">
　　　　	 <div class="col-xs-9">
			 <table class="table table-hover table-inverse">
				<tbody>

					<tr>
						<%= @sub_materials.each do |material|%>
						<td><%= attachment_image_tag(material, :image, size:'80x80', class: "img-circle pull-left profile-thumb", fallback: "5.png")%></td>
						<td><%= material.title %></td>
						<td><%= material.body %></td>
						<% end %>
					</tr>

				</tbody>
			</table>
		</div>
	</div>
</div>
